package algorithms.array.basic;

public class Swap {
    //basic swap operation
    void swap(int[] arr, int i, int j) {
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}

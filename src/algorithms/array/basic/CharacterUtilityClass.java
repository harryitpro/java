package algorithms.array.basic;

/**
 * Built-in Character Class (Java Standard)
 * The Character class provides many static methods for working with characters. Some useful methods include:
 * digit or letter?
 *      isDigit(ch)
 *      isLetter(ch)
 *      isLetterOrDigit(ch)
 *      isWhitespace(ch)
 * is lowercase or uppercase?
 *      isLowerCase(ch)
 *      isUpperCase(ch)
 *      toLowerCase(ch)
 *      toUpperCase('b')
 */
public class CharacterUtilityClass {


}

package algorithms.datasturctures.basic;

public class SetBasics {
    /**
     * In Java, the Set interface is part of the java.util package and represents an unordered collection of unique elements. The most commonly used implementations are:
     *
     * HashSet (fast, unordered)
     * LinkedHashSet (maintains insertion order)
     * TreeSet (sorted order)
     * Commonly Used Set Methods in Java
     * Method	Description
     * add(E e)	Adds an element to the set (returns false if already present).
     * addAll(Collection<? extends E> c)	Adds all elements from another collection.
     * remove(Object o)	Removes the specified element.
     * contains(Object o)	Checks if the set contains a specific element.
     * size()	Returns the number of elements in the set.
     * isEmpty()	Checks if the set is empty.
     * clear()	Removes all elements.
     * iterator()	Returns an iterator to traverse elements.
     * toArray()	Converts the set to an array.
     * retainAll(Collection<?> c)	Retains only the elements in the specified collection (intersection).
     * removeAll(Collection<?> c)	Removes all elements in the specified collection (difference).
     * forEach(Consumer<? super E> action)	Iterates using a lambda function.
     */
}
